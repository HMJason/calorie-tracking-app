I have a detailed implementation plan and task list for a modern Calorie Tracking App.
I want you to implement the project following this plan.

Here is the context:
- The goal is to build a premium, glassmorphism-styled web app.
- We are using Next.js (App Router), TypeScript, and Vanilla CSS (no Tailwind).
- The focus is on the frontend implementation of the dashboard, image upload, and analytics views.
- **Backend**: We are using Firebase (Project ID: `calorie-tracking-app-b4c7a`).
  - Authentication (Email/Password) is initialized.
  - Firestore Database is initialized.
  - Please implement the `users` and `dailyLogs` collections as described in the plan.
  - **Configuration**: Use the credentials in `firebase_config.ts` (included in this folder).

Please verify the files `implementation_plan.md` and `task.md` in this directory for the exact details and requirements.
You can also refer to `VERIFICATION.md` to see the backend status.
Start by initializing the Next.js project as described in the plan.
